# WeIdea Studio — update channel

Built artifacts only: the version manifest (`weidea-studio.json`) and
release packages (`zips/`). Served via GitHub Pages at
`https://blazzzar.github.io/weidea-updates/weidea-studio.json`.

Published by `bin/publish-channel.sh` in the (private) source repo —
do not edit by hand. Recent releases are retained here to power the
plugin's one-click rollback.
